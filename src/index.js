import React from "react";
import ReactDOM from "react-dom/client";
import InnerWorld from "./InnerWorld";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<InnerWorld />);
